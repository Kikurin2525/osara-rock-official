# グレーへの調整と軽量化（2026-09-15）

## 変更

- 黒に近い#111から濃いグレー#3f3f3fへ、本文・見出し・ボタン・フッターを統一。ホバーは#525252。写真に重ねる暗色もグレーに変更。
- 背景色変更に合わせ、フッターの小さな白文字の濃度を上げて読みやすさを維持。ヘッダーの背景ぼかし処理を除去。
- ヒーロー6枚は幅800 / 1440 / 最大1920pxのWebPを事前生成。srcSetで端末に合うサイズを選択し、閲覧時のNext.js画像変換を経由しない。トップは原画像幅1672px以上へ拡大しない。
- コンテンツハッシュ入りファイル名と1年のimmutableキャッシュ。更新時は別URLになる。
- Noto Sans JPはサイト内の文字1171字を収録したvariable WOFF2へ集約。現在の書体は保持し、未収録文字はOSフォントで表示する。ライセンスはsrc/app/fonts/OFL.txt。

## 数値と検証範囲

- CSS（非圧縮、2ファイル合計）: 136,836 bytes → 40,161 bytes（約70%削減）。
- トップの日本語フォント: HTMLの文字と旧CSSのunicode-rangeから算出した23ファイル / 448,740 bytes相当 → 1ファイル / 286,432 bytes（約36%削減）。ブラウザの実転送ログではなくソースからの推定。
- 新しいトップ画像: 800px=16,050 bytes、1440px=32,942 bytes、1672px=41,642 bytes。旧配信用原本55,778 bytes。旧Next.js変換後の各画像との直接比較ではない。
- 画像の再変換待ちを排除。画面幅390pxで800px版の選択、画像表示、メニュー開閉、各ページの横はみ出しなしを確認。
- npm run lint / npm run build（TypeScriptを含む）/ git diff --check成功。
- 公開サイトの詳細なCDP性能計測は権限が拒否されたため実行していない。秒数や体感速度の改善率は未計測。

## 再生成

- 画像: node scripts/build-hero-assets.mjs（既存のpublic/hero-*.webpから生成）
- フォント: fonttoolsとbrotliを入れたPython環境でpython3 scripts/build-font.py。新しいサイト文言を追加した際に再生成する。
